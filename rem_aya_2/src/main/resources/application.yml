cors:
  originPatterns: http://localhost:3000,http://localhost:8080,https://github.com/do5-5anto5
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rem_aya_2?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123
  profiles:
    active: development
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
    #properties:
      #hibernate:
        #dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false
  flyway:
    enabled: true
springdoc:
  pathsToMatch: /api/**/v1/**
  swagger-ui:
    use-root-path: true